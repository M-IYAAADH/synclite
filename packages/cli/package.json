{
  "name": "@nexsync/cli",
  "version": "0.1.0",
  "description": "Developer CLI for NexSync — init projects, run relay, stream logs",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "nexsync": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json && printf '#!/usr/bin/env node\\n' | cat - dist/cli.js > dist/cli.tmp.js && mv dist/cli.tmp.js dist/cli.js && chmod +x dist/cli.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nexsync/relay": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.4.0"
  },
  "files": ["dist"],
  "license": "MIT"
}
