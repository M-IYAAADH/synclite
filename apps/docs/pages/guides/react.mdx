---
title: React Guide
---

import { Callout } from 'nextra/components'

# React Guide

`@nexsync/react` wraps `@nexsync/core` with React hooks that feel native to React developers.

## Installation

```bash
npm install @nexsync/core @nexsync/react
```

## Setup

Wrap your root component (or a subtree) with `NexSyncProvider`:

```tsx
import { NexSyncProvider } from '@nexsync/react'

export default function App() {
  return (
    <NexSyncProvider
      relay="wss://relay.example.com"
      appId="my-app"
      userId="user-123"    // optional
      token="jwt-token"    // optional
      debug={false}
    >
      <MyApp />
    </NexSyncProvider>
  )
}
```

<Callout>
  Each `NexSyncProvider` creates its own NexSync instance. For multi-tenant apps or isolated panels, render multiple providers with different `appId`s.
</Callout>

## Hooks

### `useValue(key)`

Returns the current value of a key. Re-renders whenever it changes.

```tsx
function NoteTitle({ id }: { id: string }) {
  const note = useValue(`note:${id}`)
  // note is Record<string, unknown> | null

  if (!note) return <span>Loading…</span>
  return <h1>{String(note.title)}</h1>
}
```

### `useQuery(prefix)`

Returns all key-value pairs matching a prefix. Stays in sync with any changes.

```tsx
function NoteList() {
  const notes = useQuery('note:')
  // notes is Record<string, Record<string, unknown>>

  return (
    <ul>
      {Object.entries(notes).map(([key, note]) => (
        <li key={key}>{String(note.title)}</li>
      ))}
    </ul>
  )
}
```

### `useStatus()`

Returns the current connection status.

```tsx
function ConnectionBadge() {
  const status = useStatus()
  // 'connecting' | 'connected' | 'offline' | 'syncing'

  return <span className={status}>{status}</span>
}
```

### `useNexSync()`

Access the raw `NexSync` instance for writes and advanced use.

```tsx
function NoteEditor({ id }: { id: string }) {
  const db = useNexSync()
  const note = useValue(`note:${id}`)

  return (
    <textarea
      value={String(note?.body ?? '')}
      onChange={(e) => db.set(`note:${id}`, { body: e.target.value })}
    />
  )
}
```

## Complete Example

```tsx
import { NexSyncProvider, useNexSync, useValue, useQuery, useStatus } from '@nexsync/react'

function App() {
  return (
    <NexSyncProvider relay="wss://relay.example.com" appId="notes">
      <StatusBar />
      <NoteList />
    </NexSyncProvider>
  )
}

function StatusBar() {
  const status = useStatus()
  return <div>Status: {status}</div>
}

function NoteList() {
  const db = useNexSync()
  const notes = useQuery('note:')

  function addNote() {
    const id = crypto.randomUUID()
    db.set(`note:${id}`, { title: 'New note', body: '' })
  }

  return (
    <div>
      <button onClick={addNote}>Add note</button>
      {Object.entries(notes).map(([key, note]) => (
        <NoteEditor key={key} id={key} />
      ))}
    </div>
  )
}

function NoteEditor({ id }: { id: string }) {
  const db = useNexSync()
  const note = useValue(id)

  return (
    <input
      value={String(note?.title ?? '')}
      onChange={(e) => db.set(id, { ...note, title: e.target.value })}
    />
  )
}
```
